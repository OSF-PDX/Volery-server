# Volery-server

To launch the Express server, run `npm run dev` at the root of the `Volery-server` repo. This repo includes the package `nodemon`, which allows the server to update with new code changes without having to stop and restart.